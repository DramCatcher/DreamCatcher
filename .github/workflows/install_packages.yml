# DreamCatcher/.github/workflows/install_packages.yml

name: Install packages before deployment

on:
  pull_request:
    types:
      - closed
    branches:
      - dev
      - main

jobs:
  deploy:
    runs-on: [self-hosted, Linux, X64, m245, webt.int.galister.ch, debian]

    steps:
      - name: Call apt update
        run: sudo apt update
      
      - name: Install curl
        run:  sudo apt install curl -y

      - name: Get installer of node.js
        run: sudo curl -fsSL https://deb.nodesource.com/setup_20.x | sudo bash -
      
      - name: Install node.js
        run: sudo apt install nodejs -y

      - name: Install angular cli
        run: sudo npm install -g @angular/cli
